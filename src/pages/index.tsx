import type { NextPage } from "next";
import { DefaultLayout } from "../layouts/DefaultLayout";

const Home: NextPage = () => {
  return (
    <DefaultLayout>
      <div>
        <p>test</p>
      </div>
    </DefaultLayout>
  );
};

export default Home;
